# Nitro AI Judge CLI 3.1.2

3.1.2 fixes the manager's Remove images action so a fallback-backed competition changes to missing after its competition-specific image tags are deleted.

Shared fallback image digests remain cached for other competitions, and Delete workspace continues to preserve saved image metadata and workspace-independent image availability.

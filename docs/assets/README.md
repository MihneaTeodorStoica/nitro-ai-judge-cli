# 3.0 release media contract

Record these final assets with fictional seeded data:

- `tui-overview.png`: 120 by 34 terminal cells, showing all three TUI panes.
- `tui-keyboard.gif`: 8 to 12 seconds of keyboard navigation, task views, and
  contextual help.
- `play-manager.png`: 1440 by 900, showing running/healthy, stopped/ready, and
  missing competitions.
- GitHub release attachment: a silent, captioned 60 to 90 second MP4 showing
  TUI navigation; a terminal Play action appearing instantly in the open
  dashboard; progress, logs, and cancellation; opening the notebook;
  stop/restart; fallback recovery; Disconnect Nitro; and LAN Log out.

Crop browser chrome. Do not capture usernames, passwords, tokens, cookies,
CSRF values, private paths, submission details, or registry digests. Optimize
the committed PNG/GIF files to roughly 3 to 5 MB total. Once recorded, link
the committed images from README using absolute URLs pinned to `v3.0.0`; attach
the MP4 to the GitHub release instead of committing it.
